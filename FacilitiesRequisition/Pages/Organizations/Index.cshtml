@page
@model FacilitiesRequisition.Pages.Organizations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="CreateOrganization">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Organizations[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Organizations[0].IsStudentCouncil)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Organizations[0].OrganizationBudget)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Organizations) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsStudentCouncil)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrganizationBudget)
            </td>
            <td>
                <a asp-page="./EditDetails" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./ViewDetails" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./DeleteOrganization" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
